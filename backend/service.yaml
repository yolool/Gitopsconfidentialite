apiVersion: v1
kind: Service
metadata:
  name: backend-confidentielity
  namespace: backend-confidentielity
spec:
  type: ClusterIP
  selector:
    app: backend-confidentielity
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
