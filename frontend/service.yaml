apiVersion: v1
kind: Service
metadata:
  name: frontend-confidentielity
  namespace: frontend-confidentielity
spec:
  type: ClusterIP
  selector:
    app: frontend-confidentielity
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
